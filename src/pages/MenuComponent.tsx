import React from 'react';

// import config from '../config/index.json';

// const { menu } = config;

const MenuComponent = () => {
  return (
    <div>
      <div></div>
    </div>
  );
};

export default MenuComponent;
